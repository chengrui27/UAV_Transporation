分布式MPC架构
三个无人机运行同一套MPC控制器，每个无人机只考虑负载、自己和自己的绳子，将另外两个绳子的力视为外力；
在每次求解的时候，读取另外两架无人机上次求解的预测时域内的绳子拉力，作为可变的参数传入求解器的每个阶段；
每个无人机都执行同样的工作流程，他们只有绳子挂载点的位置不一样，以及另外两个外力作用的位置不一样；
为了是acados生成的求解器能够适应于每个无人机，挂载点的位置以及外力的位置应该要设置为可变的参数；

可以考虑将这个外力也纳入优化吗？
或者能否把他作为一个约束，让其他无人机也往这个力靠拢？